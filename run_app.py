"""Точка входа для собранной версии.

PyInstaller запускает указанный ему файл как самостоятельный скрипт, а
`app/main.py` — часть пакета и пользуется относительными импортами. Обёртка
берёт на себя это несоответствие: импортирует пакет как пакет и передаёт
управление внутрь.
"""
from app.main import main

if __name__ == '__main__':
    main()
